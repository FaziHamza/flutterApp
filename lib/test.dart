import 'package:flutter/material.dart';
class TestSreen extends StatelessWidget {
  const TestSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.blueAccent,),
    );
  }
}
